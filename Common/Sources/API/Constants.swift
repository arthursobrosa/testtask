import Foundation

// Enum to hold constant values used for API configuration
enum Constants {
    // The scheme for the URL (HTTP/HTTPS)
    static let scheme = "https"
    
    // The base URL of the API server
    static let baseURL = "frontend-test-assignment-api.abz.agency"
    
    // The port is optional, and in this case, it's set to nil.
    // If needed, you can provide a port number
    static let port: Int? = nil
}
